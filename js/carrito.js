let agregaralCarrito = document.getElementsByClassName("styleInfo");

for (let i = 0; i < arrayElementosMenos.length; i++) {
    if(agregaralCarrito > 0) {
        let escribe = "";
        document.querySelector(".platos-carrito").innerHTML = escribe;
    }
}